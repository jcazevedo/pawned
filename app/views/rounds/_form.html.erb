<%= simple_form_for(@round) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tournament_id %>
    <%= f.input :tournament_round_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
