<%= simple_form_for(@tournament_player) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :player_id %>
    <%= f.input :tournament_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
